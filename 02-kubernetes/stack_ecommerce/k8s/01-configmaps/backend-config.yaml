apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ecommerce
  labels:
    env: lab
    app: backend
data:
  DB_HOST: "postgres-headless"
  DB_PORT: "5432"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
