apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: ecommerce
  labels:
    app: redis
    env: lab
data:
  redis.conf: |
    maxmemory 256mb
    maxmemory-policy allkeys-lru
    appendonly no
    save ""